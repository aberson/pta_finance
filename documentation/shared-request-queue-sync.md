# Live shared request queue — delivery record

## Step 36 delivery — 2026-09-14 UTC

Candidate `7656e406eaf5e4ef89b2daac557f5465fc55f61c` adds optional queue mode to the shared fictional workflow. The same two roles can find six independently stored requests using status counts, title/reference search and owner/status filters, then open the selected request's items and attributed history. Entry, return and Reload fetch current data; a failed refresh visibly retains the last valid list. Comments, decisions and completion use each selected request's existing transaction/retry lane.

The original packaged source, schema, request path and exact receipts remain unchanged. Five validated fictional sources seed only missing documents with no invented decisions. The list returns bounded summaries without identities or comment bodies; every row comes from its validated full history under a shared deadline. The wheel and declared image inventory include all six sources and queue assets.

Both isolated candidate and integrated main passed the complete repository suite: **1,231 collected, 1,228 passed, 3 unchanged existing skips**. All **281 web tests** passed without skips. Strict package mypy, Ruff lint/format, distribution build and private-denylist identity checks passed. The installed-wheel HTTP/Chromium/emulator smoke passed legacy comments and handoff branches plus independent queue mutation/restart and original-receipt preservation. All six fresh independent review lenses passed in iteration 10; original gate/review logs remain in private evidence. All three [feature CI jobs](https://github.com/aberson/pta_finance/actions/runs/34823870771) passed.

Integration uses Git's three-way merge and preserves the operator's unrelated `.gitignore` edit byte-for-byte and unstaged. Step 36 / #68 is code delivery; Phase 8 / #67 and Step 37 / #69 remain open for M8. Detailed build, account and namespace values remain private.

**Hosted queue acceptance is pending.** No M8 image build, deployment or two-account observation is claimed by these code gates. The prepared runbook preserves M7 and specifies real decisions, six-history isolation, disabled-user restoration, fresh-revision persistence and the operator's functional judgment. Ticket creation and lifecycle engineering follow accepted M8 under the already approved product rules.
