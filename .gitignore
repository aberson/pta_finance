# --- Secrets & private config (NEVER commit; this is a public repo) ---
config.toml
secrets/
service-account*.json
*-credentials.json
.env

# --- Generated data (kept private / out of the repo) ---
snapshots/
reports/output/

# --- Private treasurer/email samples & PII source docs (NEVER commit) ---
# Real receipts/emails carry names, addresses, order + card fragments. Committed
# docs belong under documentation/ or docs/, not here.
doc/
mail_samples/
*.eml
*.mbox

# --- Python ---
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Local tooling ---
.claude/settings.local.json
.plan-expedite-state
# local session/handoff state — may contain private financials; never publish
.claude/task-state/
